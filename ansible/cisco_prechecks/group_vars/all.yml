ansible_user: "{{ vault_ansible_user }}"

ansible_password: "{{ vault_ansible_password }}"

ansible_connection: network_cli

ansible_network_os: cisco.ios.ios

cpu_threshold: 75

minimum_flash_space: 5000000000
